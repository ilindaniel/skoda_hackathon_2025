services:
  db:
    image: postgres:13
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=mydb
    ports:
      - 5432:5432
#    volumes:
#      - exposed-db:/var/lib/postgresql/data/

#volumes:
#  exposed-db: